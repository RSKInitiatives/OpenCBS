using TinyMessenger;

namespace OpenCBS.ArchitectureV2.Message
{
    public class SearchShownMessage : ITinyMessage
    {
        public SearchShownMessage(object sender)
        {
            Sender = sender;
        }

        public object Sender { get; private set; }
    }
}
